# --- Frontend (Next.js) environment ---
# Copy to .env.local for local dev. In production set this in your host dashboard.

# Base URL of the deployed Express backend (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:5001
